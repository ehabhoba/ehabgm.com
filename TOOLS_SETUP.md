# دليل إعداد أدوات الذكاء الاصطناعي

## نظرة عامة

يحتوي الموقع على 9 أدوات احترافية، جميع أدوات الذكاء الاصطناعي تعمل الآن بـ **Google Gemini API** (المجاني).

---

## الأدوات التي تعمل بدون إعداد ✅

هذه الأدوات جاهزة للاستخدام فوراً:

1. **مولد رموز QR** - يستخدم API عامة مجانية
2. **مولد روابط واتساب** - معالجة من جانب العميل
3. **مولد الفواتير** - معالجة من جانب العميل
4. **مولد شهادات NFT** - معالجة من جانب العميل
5. **اختصار الروابط** - يعمل مع تخزين محلي (يحتاج قاعدة بيانات للإنتاج)

---

## أدوات الذكاء الاصطناعي (Google Gemini) ⚙️

### جميع الأدوات التالية تستخدم Google Gemini API:

1. **منشئ المواقع بالذكاء الاصطناعي**
2. **مولد الأبحاث الأكاديمية**
3. **الدردشة الذكية**
4. **محول الشعارات إلى 3D**
5. **مولد المقالات التلقائي**

**المتطلبات:**
- مفتاح Google API (مجاني)

**خطوات الإعداد:**

#### الطريقة 1: استخدام Vercel (موصى به)
1. افتح قسم **Vars** في الشريط الجانبي للدردشة
2. أضف متغير بيئة جديد:
   - الاسم: `GOOGLE_API_KEY`
   - القيمة: مفتاح API الخاص بك من Google AI Studio
3. احفظ التغييرات

#### الطريقة 2: ملف محلي
1. أنشئ ملف `.env.local` في جذر المشروع
2. أضف السطر التالي:
   ```
   GOOGLE_API_KEY=your-google-api-key-here
   ```
3. أعد تشغيل خادم التطوير

**الحصول على مفتاح Google API (مجاني):**
1. اذهب إلى [Google AI Studio](https://aistudio.google.com/app/apikeys)
2. سجل الدخول بحساب Google
3. انقر على "Create API Key"
4. اختر المشروع أو أنشئ واحداً جديداً
5. انسخ المفتاح واحفظه بأمان

---

## النماذج المستخدمة

| الأداة | النموذج | الوصف |
|--------|---------|-------|
| الدردشة الذكية | gemini-2.0-flash | محادثة سريعة ودقيقة |
| مولد الأبحاث | gemini-2.0-flash | إنشاء أبحاث أكاديمية |
| منشئ المواقع | gemini-2.0-flash | توليد كود مواقع |
| محول 3D | gemini-1.5-flash | تحليل وتحويل الصور |
| مولد المقالات | gemini-2.0-flash | كتابة مقالات SEO |

---

## حدود الاستخدام المجاني

### Google Gemini API (مجاني)
- **60 طلب/دقيقة** للنموذج gemini-2.0-flash
- **1500 طلب/يوم** للاستخدام المجاني
- لا توجد رسوم للاستخدام المحدود

### نصائح لتحسين الاستخدام:
1. تجنب إرسال طلبات متكررة سريعة
2. استخدم الحفظ المحلي للنتائج
3. راقب استخدامك في Google AI Studio

---

## ترقية اختصار الروابط للإنتاج

**الوضع الحالي:** يخزن الروابط في الذاكرة (تُفقد عند إعادة التشغيل)

**للترقية:**

### الخيار 1: استخدام Supabase (موصى به)
1. من قسم **Connect** في الشريط الجانبي، أضف Supabase
2. أنشئ جدول `shortened_urls`:
   ```sql
   CREATE TABLE shortened_urls (
     id SERIAL PRIMARY KEY,
     slug VARCHAR(50) UNIQUE NOT NULL,
     original_url TEXT NOT NULL,
     clicks INTEGER DEFAULT 0,
     created_at TIMESTAMP DEFAULT NOW()
   );
   ```

### الخيار 2: استخدام Upstash Redis
1. من قسم **Connect**، أضف Upstash for Redis
2. سيتم تكوين المتغيرات تلقائياً

---

## التحقق من الإعداد

بعد إضافة المتغيرات:
1. أعد تحميل الصفحة
2. جرب الأداة
3. إذا ظهرت رسالة "لم يتم تكوين مفتاح API"، تحقق من:
   - صحة اسم المتغير (`GOOGLE_API_KEY`)
   - صحة قيمة المفتاح
   - إعادة نشر المشروع (إذا كان على Vercel)

---

## مقارنة التكاليف

| المزود | التكلفة | الحد المجاني |
|--------|---------|--------------|
| Google Gemini | مجاني | 60 طلب/دقيقة، 1500/يوم |
| OpenAI GPT-4 | $0.03/1K tokens | لا يوجد |
| Claude | $0.015/1K tokens | محدود |

**الخلاصة:** Google Gemini هو الخيار الأفضل للتكلفة والأداء!

---

## الدعم

إذا واجهت مشاكل:
1. تحقق من سجلات الأخطاء في Console
2. تأكد من صحة مفتاح GOOGLE_API_KEY
3. تحقق من حدود الاستخدام في Google AI Studio
4. اتصل بالدعم الفني

---

## ملاحظات أمنية ⚠️

- **لا تشارك** مفاتيح API أبداً
- **لا تضع** المفاتيح في الكود المصدري
- استخدم متغيرات البيئة دائماً
- قم بتدوير المفاتيح بانتظام
- راقب الاستخدام لاكتشاف أي نشاط غير عادي
```

```plaintext file="public/robots.txt"
# robots.txt for ehabgm.com

User-agent: *
Allow: /
Disallow: /api/
Disallow: /_next/
Disallow: /admin/
Disallow: /private/

User-agent: Googlebot
Allow: /

User-agent: Googlebot-Image
Allow: /

User-agent: Bingbot
Allow: /

Sitemap: https://ehabgm.com/sitemap.xml
Host: https://ehabgm.com
